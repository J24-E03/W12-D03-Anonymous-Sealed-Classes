package tasks.model;

public class LoanAccount implements Account {
    // Account is a sealed interface and no class can implement this interface.
    // In order to implement the interface, you have to add your class name after the "permits" keyword in the Account interface declaration.
}
